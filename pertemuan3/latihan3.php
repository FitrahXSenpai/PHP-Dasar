<?php

// Function
// Function singkatnya adalah potongan program atau baris-baris kode yang dibuat untuk memperpudah kita pada saat mempogram, kode program tadi bisa kita kasih nama dan bisa kita panggil berulang-ulang.

// ada 2 jenis Function yaitu
// 1. Built-in Function (fungsi yang sudah disiapkan oleh PHP).

	// Built-in Function
		// https://www.php.net/manual/en/funcref.php

			// Function Date / Time
				// time()
				// date()
				// mktime()
				// strtotime()

			// String
				// strlen() 
					// untuk menghitung panjang sebuah string
				// strcmp() 
					// untuk membandingkan sebuah string
				// explode() 
					// untuk memecah sebuah string menjadi array
				// htmlspecialchars() 
					// untuk menjaga dari orang jahat yang ingin masuk ke website kita

			// Utility (Bantuan)
				// var_dump()
					// untuk mencetak isi dari sebuah variable, array, objek
				// isset()
					// untuk mengecek apakah suatu variable sudah pernah di buat atau belum yang akan menghasilkan nilai boolean true or false
				// empty()
					// untuk mengecek apakah variable yang ada itu ada isinya atau tidak
				// die()
					// untuk menghentikan program
				// sleep()
					// untuk menghentikan program sementara

// 2. User-Defined Function (fungsi yang dibuat oleh kita sendiri).


?>